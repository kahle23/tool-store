# HTTP client debug server

HTTP client debug server is mainly used to debug HTTP client tools or some problems when sending ajax.

You can call any address (except '/' and '/index') and the server will return information about the request, such as how to pass parameters, for debugging ajax requests or HTTP client tool development or debugging.


